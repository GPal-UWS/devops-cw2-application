services:
  run-api-app:
    build:
      context: assignment_2_app
      dockerfile: Dockerfile
  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - run-api-app
